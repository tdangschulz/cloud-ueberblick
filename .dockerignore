# Allowlist statt Blocklist: ALLES ausschließen, dann nur die Dateien
# erlauben, die die App tatsächlich zur Laufzeit braucht. So kopiert
# COPY . . im Dockerfile nie versehentlich CloudFormation-/Terraform-
# Übungsdateien, Doku, .zip-Archive o.ä. mit ins Image – egal was im
# Projektordner sonst noch an Schulungsmaterial dazukommt.
*

!package.json
!package-lock.json
!server.js
!app.js
